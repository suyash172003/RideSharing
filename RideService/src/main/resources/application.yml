server:
  port: 8083

spring:
  application:
    name: RIDE-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/RideService?createDatabaseIfNotExist=true
    username: root
    password: Saurabh@645
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

