server:
  port: 8081

spring:
  application:
    name: UserService

  datasource:
    username: root
    password: Saurabh@645
    url: jdbc:mysql://localhost:3306/RideSharing?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
