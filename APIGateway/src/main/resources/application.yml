spring:
  application:
    name: API-GATEWAY
  cloud:
    gateway:
      mvc:
        routes:



server:
  port: 9090

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
